name: cli
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.1 <3.0.0'

executables:
  cli-dev: dev_script

dependencies:
  cli_dialog: ^0.5.0
  slug: ^1.0.0

dev_dependencies:
  lints: ^1.0.0
